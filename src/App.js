// import the component before using it
import ExpenseItem from "./components/ExpenseItem.js";

function App() {
  return (
    <div className="app-container">
      {/* create component ExponentItem and add that here. */}
      <ExpenseItem />
    </div>
  );
}

export default App;
