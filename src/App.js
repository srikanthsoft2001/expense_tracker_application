// import the css
import "./App.css";

// write the boiler plate

// function with the same name
function App() {
  return (
    <div className="app-container">
        
    </div>
  );
}

// do the export default
export default App;

// Parsing error: Adjacent JSX elements must be wrapped in an enclosing tag
// pass it in a wrapper like a div
